//Core
import { put, call, apply } from 'redux-saga/effects';

// Actions
import { mapActions } from '../../../map/actions';

export function* createPolyline ({ payload : ref }) {

  try {

  } catch (err) {
    console.log('err', err);
  }
}
