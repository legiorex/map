// Types
import { types } from './types';
const initialState = {};

export const googleReducer = (state = initialState, action) => {
  switch (action.type) {
    // case types.GOOGLE_INIT:
    //   console.log('action.payload', action.payload);

    //   return state;

    default:
      return state;
  }
};
